<p id="notice"><%= notice %></p>

<table>
  <thead>
    <tr>
      <th>User</th>
      <%# <th>Shift</th> %>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @assignments.each do |assignment| %>
      <tr>
        <td><%= assignment.username %></td>
        <td><%= assignment.shift_id %></td>
        <td><%= link_to 'Show', assignment %></td>
        <td><%=link_to 'Sign Out', assignment, method: :delete, data: { confirm: 'Are you sure?' }%></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Sign In', new_assignment_path %>
